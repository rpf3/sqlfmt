declare @count int;

declare @name varchar(50) = 'default';

declare @total int = 0;

declare
	@count int
,	@name varchar(50);

declare
	@count int
,	@name varchar(50)
,	@total int;

declare @results table
(
	id int not null
,	name varchar(50) not null
);
